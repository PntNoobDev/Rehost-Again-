<sap:ActivityDesigner x:Class="Rehost_Again_.SmtpActivityDesigner"
                      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                      xmlns:sap="clr-namespace:System.Activities.Presentation;assembly=System.Activities.Presentation"
                      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                      mc:Ignorable="d" Height="234" Width="288">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="To" Margin="5"/>
        <TextBox Text="{Binding Path=ModelItem.Properties[To].Value, Mode=TwoWay}" Grid.Row="0" Margin="5" />

        <TextBlock Text="Subject" Grid.Row="1" Margin="5"/>
        <TextBox Text="{Binding Path=ModelItem.Properties[Subject].Value, Mode=TwoWay}" Grid.Row="1" Margin="5" />

        <TextBlock Text="Body" Grid.Row="2" Margin="5"/>
        <TextBox Text="{Binding Path=ModelItem.Properties[Body].Value, Mode=TwoWay}" Grid.Row="2" Margin="5" />
    </Grid>
</sap:ActivityDesigner>
